@page "/"
@*
    Landing page (#196). Built entirely from the shared design-system CSS
    primitives (.panel / .cnr brackets / .tag / .btn / .telemetry-strip) so it
    matches the live dashboard. Static SSR — no interactivity.
*@

<PageTitle>SuperStatus — status pages you own</PageTitle>

<main class="main lp-main">

    @* HERO — the single white-bracket primary frame on the page *@
    <section class="panel primary frame lp-hero">
        <span class="cnr tl"></span><span class="cnr tr"></span>
        <span class="cnr bl"></span><span class="cnr br"></span>
        <div class="callsign" style="margin-bottom: var(--s-2)">
            <span class="label">SUPERSTATUS</span><span class="sep">//</span><span class="value">operational telemetry, owned by you</span>
        </div>
        <h1>Status pages you <em>own.</em></h1>
        <p class="lede">
            A clean, modern status page for your services, incidents and uptime —
            self-host it for free today. A fully-managed option,
            <strong>SuperStatus Cloud</strong>, is coming soon.
            No lock-in, no per-seat surprises.
        </p>
        <div class="telemetry-strip">
            <div class="chip"><span class="k">Self-host</span><span class="v accent">€0</span></div>
            <div class="chip"><span class="k">SuperStatus Cloud</span><span class="v">Soon</span></div>
            <div class="chip"><span class="k">Source</span><span class="v accent">Available</span></div>
            <div class="chip"><span class="k">Stack</span><span class="v">.NET · Blazor</span></div>
        </div>
        <div class="lp-cta-row">
            <a class="btn primary" href="https://status.superstatus.io" target="_blank" rel="noopener">Live demo →</a>
            <a class="btn" href="#quickstart">Get started</a>
        </div>
    </section>

    @* PREVIEW — framed screenshot of the live public dashboard *@
    <section class="panel frame lp-preview">
        <span class="cnr tl"></span><span class="cnr tr"></span>
        <span class="cnr bl"></span><span class="cnr br"></span>
        <div class="callsign">
            <span class="label">LIVE PREVIEW</span><span class="sep">//</span><span class="value">your public dashboard</span>
            <span class="meta">status.superstatus.io</span>
        </div>
        <a class="lp-preview-stage" href="https://status.superstatus.io" target="_blank" rel="noopener">
            <img src="img/preview-dashboard.png" alt="SuperStatus public dashboard — services, uptime strips and incident log in a clean, modern interface" loading="lazy" />
        </a>
        <div class="lp-preview-cap">
            <span>30-day uptime · live incidents · per-service latency</span>
            <a href="https://status.superstatus.io" target="_blank" rel="noopener">View live demo →</a>
        </div>
    </section>

    @* CAPABILITIES *@
    <section class="lp-sec">
        <div class="callsign">
            <span class="label">CAPABILITIES</span><span class="sep">//</span><span class="value">what you get</span>
        </div>
        <div class="lp-feature-grid">
            <div class="panel frame lp-feature">
                <span class="cnr tl"></span><span class="cnr tr"></span><span class="cnr bl"></span><span class="cnr br"></span>
                <span class="tag accent"><span class="led up"></span>UPTIME</span>
                <h3>Uptime checks</h3>
                <p>HTTP, TCP and keyword checks on your schedule, with 30-day history strips per service.</p>
            </div>
            <div class="panel frame lp-feature">
                <span class="cnr tl"></span><span class="cnr tr"></span><span class="cnr bl"></span><span class="cnr br"></span>
                <span class="tag"><span class="led down"></span>INCIDENTS</span>
                <h3>Incident timeline</h3>
                <p>Post incidents, track severity and resolution, and keep a public, honest history.</p>
            </div>
            <div class="panel frame lp-feature">
                <span class="cnr tl"></span><span class="cnr tr"></span><span class="cnr bl"></span><span class="cnr br"></span>
                <span class="tag accent">DESIGN</span>
                <h3>Modern look &amp; feel</h3>
                <p>A clean, modern interface that's easy to read at a glance — looks and feels the part, not another bland dashboard.</p>
            </div>
            <div class="panel frame lp-feature">
                <span class="cnr tl"></span><span class="cnr tr"></span><span class="cnr bl"></span><span class="cnr br"></span>
                <span class="tag accent">SOURCE</span>
                <h3>Self-hostable</h3>
                <p>One container stack, Docker-compose ready. Run it on your own infrastructure, free — source-available under the BSL.</p>
            </div>
            <div class="panel frame lp-feature">
                <span class="cnr tl"></span><span class="cnr tr"></span><span class="cnr bl"></span><span class="cnr br"></span>
                <span class="tag">YOURS</span>
                <h3>No lock-in</h3>
                <p>Your data, your domain. Source-available — export and walk away whenever you like.</p>
            </div>
            <div class="panel frame lp-feature">
                <span class="cnr tl"></span><span class="cnr tr"></span><span class="cnr bl"></span><span class="cnr br"></span>
                <span class="tag accent">THEME</span>
                <h3>Light &amp; dark</h3>
                <p>Follows the viewer's system theme; the status colours stay load-bearing on both.</p>
            </div>
        </div>
    </section>

    @* DEPLOYMENT — self-host vs hosted *@
    <section class="lp-sec">
        <div class="callsign">
            <span class="label">DEPLOYMENT</span><span class="sep">//</span><span class="value">two ways to run it</span>
        </div>
        <div class="lp-deploy-grid">
            <div class="panel frame lp-deploy">
                <span class="cnr tl"></span><span class="cnr tr"></span><span class="cnr bl"></span><span class="cnr br"></span>
                <span class="tag"><span class="led up"></span>SELF-HOST</span>
                <h3>Run it yourself</h3>
                <ul>
                    <li>Your infrastructure, your rules</li>
                    <li>Docker-compose, up in minutes</li>
                    <li>Full source, unlimited checks</li>
                    <li>Free, forever</li>
                </ul>
            </div>
            <div class="panel frame lp-deploy">
                <span class="cnr tl"></span><span class="cnr tr"></span><span class="cnr bl"></span><span class="cnr br"></span>
                <span class="tag"><span class="led degraded"></span>SUPERSTATUS CLOUD</span>
                <h3>Let us run it <span class="lp-soon">· coming soon</span></h3>
                <ul>
                    <li>We host, patch and update it</li>
                    <li>Daily backups + TLS + custom domain</li>
                    <li>No ops on your side</li>
                    <li>Launching soon — join the waitlist</li>
                </ul>
            </div>
        </div>
    </section>

    @* QUICK START — the one-line self-host installer *@
    <section class="lp-sec" id="quickstart">
        <div class="callsign">
            <span class="label">QUICK START</span><span class="sep">//</span><span class="value">self-host in one line</span>
        </div>
        <div class="panel frame lp-install">
            <span class="cnr tl"></span><span class="cnr tr"></span><span class="cnr bl"></span><span class="cnr br"></span>
            <span class="tag accent"><span class="led up"></span>INSTALL</span>
            <h3>Run the installer</h3>
            <p>
                On any host with <a href="https://docs.docker.com/engine/install/" target="_blank" rel="noopener">Docker</a>
                and the Compose plugin, run:
            </p>
            <div class="lp-cmd">
                <span class="lp-cmd-prompt">$</span>
                <code>curl -fsSL https://superstatus.superstatus.io/install.sh | sh</code>
            </div>
            <p class="lp-install-note">
                It pulls the latest release images, generates secrets, and brings the stack
                up on plain HTTP over your LAN. When it finishes, open the printed
                <code>http://&lt;your-server-ip&gt;:8080</code> and create your admin account.
            </p>
            <ul class="lp-install-steps">
                <li>No build, no source checkout — prebuilt images, up in minutes</li>
                <li>Update any time with <code>docker compose pull &amp;&amp; docker compose up -d</code></li>
                <li>Internet-facing? Put it behind a TLS reverse proxy first</li>
            </ul>
            <div class="lp-cta-row">
                <a class="btn" href="/install.sh" target="_blank" rel="noopener">View the script →</a>
            </div>
        </div>
    </section>

    @* PRICING *@
    <section class="lp-sec" id="pricing">
        <div class="callsign">
            <span class="label">PRICING</span><span class="sep">//</span><span class="value">pick a lane</span>
        </div>
        <div class="lp-pricing-grid">

            <div class="panel frame lp-price">
                <span class="cnr tl"></span><span class="cnr tr"></span><span class="cnr bl"></span><span class="cnr br"></span>
                <span class="lp-tier">Self-host</span>
                <span class="lp-amount">€0 <small>/ forever</small></span>
                <ul>
                    <li>Your infrastructure</li>
                    <li>Unlimited checks</li>
                    <li>Full source code</li>
                    <li>Community support</li>
                </ul>
                <a class="btn" href="https://git.superstatus.io" target="_blank" rel="noopener">Get the source →</a>
            </div>

            <div class="panel frame lp-price lp-price-featured">
                <span class="cnr tl"></span><span class="cnr tr"></span><span class="cnr bl"></span><span class="cnr br"></span>
                <span class="lp-ribbon soon"><span class="led degraded"></span>Coming soon</span>
                <span class="lp-tier">SuperStatus Cloud</span>
                <span class="lp-amount">€9 <small>/ mo · planned</small></span>
                <ul>
                    <li>Everything in self-host</li>
                    <li>We host &amp; update it</li>
                    <li>Daily backups</li>
                    <li>TLS + custom domain</li>
                    <li>Email support</li>
                </ul>
                <a class="btn primary" href="mailto:hello@superstatus.io?subject=SuperStatus%20Cloud%20waitlist">Join the waitlist →</a>
            </div>

            <div class="panel frame lp-price">
                <span class="cnr tl"></span><span class="cnr tr"></span><span class="cnr bl"></span><span class="cnr br"></span>
                <span class="lp-tier">SuperStatus Cloud Pro <span class="lp-soon">· soon</span></span>
                <span class="lp-amount">€29 <small>/ mo · planned</small></span>
                <ul>
                    <li>Everything in Cloud</li>
                    <li>More checks &amp; history</li>
                    <li>Team seats</li>
                    <li>Priority support</li>
                </ul>
                <a class="btn" href="mailto:hello@superstatus.io?subject=SuperStatus%20Cloud%20Pro%20waitlist">Join the waitlist →</a>
            </div>

        </div>
    </section>

</main>
