@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

@* Issue #151 Phase 2: HUD welcome card. *@
<div class="hud-auth-card">
    <div class="hud-auth-head">
        <span class="tag accent"><span class="led up"></span>Identity provider</span>
    </div>
    <p class="hud-auth-intro">The SuperStatus identity provider is online and ready.</p>
</div>
